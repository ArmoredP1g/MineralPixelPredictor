Model files

- The model is defined in `models/models.py` (network and related implementations).

Monte Carlo sampling code

- The Monte Carlo sampling is implemented in the dataloader in `dataloaders/dataloaders.py`.

Training flow

- The training flow is in `train.py` (main training script) and `trainer.py` (training logic/wrapper).
